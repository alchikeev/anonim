services:
  anonim-web:
    container_name: anonim-web
    build:
      context: .
      args:
        DJANGO_ENV: prod
    env_file:
      - .env.prod
    expose:
      - "8000"
    volumes:
      - /srv/data_anonin:/data
      - /srv/media_anonin:/app/media
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
